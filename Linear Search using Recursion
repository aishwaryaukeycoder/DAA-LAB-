#include<stdio.h>
void read1(int arr[], int n){
	printf("Enter %d elements : ",n);
	for(int i=0; i<n; i++){
		scanf("%d", &arr[i]);
	}
}

int linearSearch(int arr[], int n, int index, int i){
	if (n>index)
		return -1;
	if(arr[n]==i)
		return n;
	return linearSearch(arr, n+1, index, i);
}
